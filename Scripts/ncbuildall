#!/usr/bin/env bash
# See License.md in the project root for license information.
#
SCRIPT=$(cd $(dirname $BASH_SOURCE) && pwd)
. $SCRIPT/_inc

err=

_opt_debug=1
_opt_dynamic=0
_opt_native=0
_opt_clean=0

opt_debug() {
    [ $_opt_debug -ne 0 ] && return 0
    return 1
}

opt_dynamic() {
    [ $_opt_dynamic -ne 0 ] && return 0
    return 1
}

opt_native() {
    [ $_opt_native -ne 0 ] && return 0
    return 1
}

opt_clean() {
    [ $_opt_clean -ne 0 ] && return 0
    return 1
}

get_options() {
    eval set -- "$(getopt -o cNdnvh -l clean,native,dynamic,nodebug,verbose,help -- "$@")"

    while true
    do
	    case "$1" in
            -c|--clean)
                _opt_clean=1
                shift
                ;;
            -N|--native)
                _opt_native=1
                shift
                ;;
            -n|--nodebug)
                _opt_debug=0
                shift
                ;;
            -v|--verbose)
                _opt_verbose=1
                shift
                ;;
            -d|--dynamic)
                _opt_dynamic=1
                shift
                ;;
            -h|--help)
                echo "Usage: $0 [options]"
                echo "Options:"
                echo " -c, --clean            Clean build and install directories before building"
                echo "  -N, --native          Build for native execution in Linux (no cross-compilation)"
                echo "  -d, --dynamic         Build with shared libraries (default is static)"
                echo "  -n, --nodebug         Build without debug symbols and features"
                echo "  -v, --verbose         Enable verbose output"
                echo "  -h, --help            Show this help message and exit"
                shift
                exit 0
                ;;
	    --)
		    shift
		    break
		    ;;
	    *)
		    err="$err $1"
		    shift
		    break
		    ;;
	    esac
    done
}


in_container || _fail "Not running in devcontainer." 

get_options $@
[ -n "$err" ] && _fail "Invalid options: $err"

addopts=
! opt_debug && addopts="$addopts --nodebug"
opt_dynamic && addopts="$addopts --dynamic" 
opt_clean && $addopts="$addopts --clean"

if opt_native; then
    _trace "Building for native execution in Linux (no cross-compilation)"
    ncbuild $addopts
    ncbuild $addopts --ascii
    ncbuild $addopts --reentrant
    ncbuild $addopts --interop
    ncbuild $addopts --spfuncs
    ncbuild $addopts --ascii --reentrant
    ncbuild $addopts --ascii --interop
    ncbuild $addopts --ascii --spfuncs
    ncbuild $addopts --reentrant --interop
    ncbuild $addopts --reentrant --spfuncs
    ncbuild $addopts --interop --spfuncs
    ncbuild $addopts --ascii --reentrant --interop
    ncbuild $addopts --ascii --reentrant --spfuncs
    ncbuild $addopts --ascii --interop --spfuncs
    ncbuild $addopts --reentrant --interop --spfuncs
    ncbuild $addopts --ascii --reentrant --interop --spfuncs
else
    _trace "Building for Windows using cross-compilation"
    for p in "" "--msvcrt" "--x86 --msvcrt" "--woa"
    do
        ncbuild $addopts $p
        ncbuild $addopts $p --ascii
        ncbuild $addopts $p --reentrant
        ncbuild $addopts $p --ascii --reentrant
    done
fi
exit 0
